#include <iostream>
#include <vector>
using namespace std;

int main(void) {
	int N;
	cin >> N;
	vector<int> W(N), H(N), B(N);
	for (int i = 0; i < N; ++i) cin >> W[i] >> H[i] >> B[i];
	// Wの
	// 部品が一つの時は体につける
	return 0;
}